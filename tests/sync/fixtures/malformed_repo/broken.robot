*** Settings
Library    SeleniumLibrary
this line has no section and no valid setting syntax at all

*** Test Cases ***
    this test case name is indented, which is invalid Robot syntax
Another One
this line is not indented under it, also invalid
