class AdminDashboardPage:
    """Page Object for the existing Admin Portal UI — the second of two
    existing UIs sharing the same backend in this sanitized fixture, so
    a capability catalog built from this repository must never assume
    'one UI = one API = one DB = one framework'.
    """

    def open(self):
        pass
