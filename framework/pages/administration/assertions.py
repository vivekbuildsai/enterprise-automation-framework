from __future__ import annotations

from framework.assertions import UIAssert


class AdministrationAssertions(UIAssert):
    """No domain-specific checks yet — see `ReportAssertions` for why this
    is a subclass rather than an empty new class.
    """
