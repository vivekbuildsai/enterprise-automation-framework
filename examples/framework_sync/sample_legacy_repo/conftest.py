# This directory is fixture DATA for Example C (a sample repository for
# `RepositoryAnalyzer` to analyze), not part of this framework's own test
# suite — tell pytest to never try to collect anything under here.
collect_ignore_glob = ["*"]
