validation_mode: ui_api

ui:
  base_url: "${AUTOMATION_UI_BASE_URL:-https://qa.example.com}"
  login_username: "${AUTOMATION_UI_USERNAME:-}"
  login_password: "${AUTOMATION_UI_PASSWORD:-}"

api:
  subscriber_management:
    base_url: "${AUTOMATION_API_SUBSCRIBER_URL:-https://api.qa.example.com/subscriber}"
    timeout_seconds: 30
    auth_type: bearer
    client_id: "${AUTOMATION_API_CLIENT_ID:-}"
    client_secret: "${AUTOMATION_API_CLIENT_SECRET:-}"

database:
  subscriber_db:
    enabled: true
    dialect: postgresql
    host: "${AUTOMATION_DB_HOST:-qa-db.example.com}"
    port: 5432
    database: "${AUTOMATION_DB_NAME:-sample_app}"
    username: "${AUTOMATION_DB_USER:-automation_qa}"
    password: "${AUTOMATION_DB_PASSWORD:-}"
    pool_size: 10

browser:
  browser: "${AUTOMATION_BROWSER:-chromium}"
  headless: "${AUTOMATION_HEADLESS:-true}"
  slow_mo_ms: 0
  viewport_width: 1920
  viewport_height: 1080
  action_timeout_ms: "${AUTOMATION_ACTION_TIMEOUT_MS:-15000}"
  navigation_timeout_ms: "${AUTOMATION_NAVIGATION_TIMEOUT_MS:-30000}"
  trace_on_failure: true
  video_on_failure: true
  screenshot_on_failure: true
  har_recording: false

feature_flags:
  self_healing_locators: false
  ai_root_cause_analysis: false
  visual_validation: false
  api_validation: true
  database_validation: false

parallel_workers: 8
